general_parameters:
  user: Oli
  atlas_registration: false
  save_2D: false
  save_3D: true
  orientation_acquisition: IPL # Choose None if you do not want to reorient your sample
  downsampling: 8
  # The following general parameters are automatically set
  input_path: 
  output_dir: 
  yaml_parameter_file: 
bigstitcher:
    reader: fast
    fusion: fast
    xml_file: 
    channel_alignment_parameters:
      pairwise_shifts_downsamples:
        x: 4
        y: 4
        z: 2
      filter_min_r: 0.7
    tile_alignment_parameters:
      use_channel: 0
      pairwise_shifts_downsamples:
        x: 4
        y: 4
        z: 2
      filter_min_r: 0.7
    icp_refinement_parameters:
      icp_refinement_type: Simple (all together)
      downsampling: Downsampling 8/8/4
      interest: Average Threshold
      icp_max_error: Normal Adjustment (<5px)
    fusion_parameters:
      fusion_type: bigstitcher
      preserve_original: #preserve_original # Or select nothing, especially if you reorient your sample
      produce: Each timepoint & channel
      fused_image: Save as (compressed) TIFF stacks
      fusion_method: "SMOOTH AVERAGE"
      fuse_dir: fused
brainreg:
    docker_container_name: biop-brainreg